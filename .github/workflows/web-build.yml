name: Build expense tracker web app

on:
  push:
    paths:
      - 'expense-tracker/**'
      - '.github/workflows/web-build.yml'
  pull_request:
    paths:
      - 'expense-tracker/**'
      - '.github/workflows/web-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-web:
    name: Test and build web app
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: expense-tracker
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: expense-tracker/package-lock.json

      - name: Install dependencies
        # The lockfile was produced on Windows and contains platform-specific
        # optional packages. npm install resolves the correct Linux variants.
        run: npm install --no-audit --no-fund

      - name: Run tests
        # Keep the runner deterministic for Firebase mocks and browser-oriented packages.
        run: npm test -- --maxWorkers=1 --minWorkers=1

      - name: Type check
        run: npx tsc -b

      - name: Build production bundle
        run: npm run build

      - name: Upload production bundle
        uses: actions/upload-artifact@v4
        with:
          name: expense-tracker-web-${{ github.sha }}
          path: expense-tracker/dist
          if-no-files-found: error
          retention-days: 14
